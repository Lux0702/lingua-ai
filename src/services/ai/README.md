# AI Service

This module provides a single interface between the frontend and the AI Agent.

Responsibilities:

- Send requests to the AI Agent.
- Receive structured JSON.
- Validate responses.
- Never contain UI logic.
- Never contain React components.hiểu